@model IEnumerable<WebApp.Models.ComputerSkill>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ComputerSkillName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ComputerSkillLevel)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ComputerSkillName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ComputerSkillLevel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ComputerSkillId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ComputerSkillId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ComputerSkillId })
        </td>
    </tr>
}

</table>
