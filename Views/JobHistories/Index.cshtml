@model IEnumerable<WebApp.Models.JobHistory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.JobHistoryName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mission)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WorkDays)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Reason4Leaving)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.JobHistoryName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mission)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WorkDays)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Reason4Leaving)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.JobHistoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.JobHistoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.JobHistoryId })
        </td>
    </tr>
}

</table>
