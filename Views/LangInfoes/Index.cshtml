@model IEnumerable<WebApp.Models.LangInfo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LangInfoName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LangInfoLevel)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LangInfoName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LangInfoLevel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LangInfoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LangInfoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LangInfoId })
        </td>
    </tr>
}

</table>
